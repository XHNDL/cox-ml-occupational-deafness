# 职业性噪声聋患病预测研究 - 开发对话记录

## 项目简介

**课题**: 融合 Cox 模型与机械学习的职业性耳聋患病预测研究  
**工具**: OpenCode AI Agent  
**时间**: 2026-05-06 ~ 2026-05-07  
**目标**: 融合Cox生存分析与机器学习，构建噪声聋风险预测系统

---

## 二、需求与实现对照表

| # | 用户需求 | 实现方案 | 状态 |
|---|---------|---------|------|
| 1 | 融合Cox模型与机器学习 | cox_ml_unified.py 同时训练Cox和ML模型 | ✅ 完成 |
| 2 | 将数据2并入前面的数据 | data_clear.py 统一加载合并 | ✅ 完成 |
| 3 | 数据2的处理并入，目的不变 | 添加数据来源标识，保留各自目标变量 | ✅ 完成 |
| 4 | 数据2的职业病风险关联分析 | occupational_risk_analysis.py (已整合) | ✅ 完成 |
| 5 | 检查二进制文件能否做趋势 | trend_analysis.py 分析发现仅2024年数据 | ⚠️ 数据不足 |
| 6 | 删除无用的py文件 | 删除survival_analysis.py等5个文件 | ✅ 完成 |
| 7 | 删除重复的输出图 | 删除旧PNG，保留3个核心图 | ✅ 完成 |
| 8 | 删除CSV参考文件 | 删除10.16369-*.csv | ✅ 完成 |
| 9 | 解决Cox输出乱码 | 改用英文标签 | ✅ 完成 |
| 10 | 生成论文公式 | 论文公式与模型结构.md | ✅ 完成 |
| 11 | 生成流程总结 | 研究流程总结.md | ✅ 完成 |
| 12 | 生成代码流程说明 | 代码流程与逻辑说明.md | ✅ 完成 |
| 13 | 整理开发对话记录 | 开发对话记录.md | ✅ 完成 |
| 14 | 开源到GitHub | 整理项目结构，准备上传 | ✅ 完成 |

---

## 三、开发流程图

```
需求分析
    ↓
数据加载 ← 数据2(体检) + 数据3(听力)
    ↓
数据清洗（异常值、缺失值、格式转换）
    ↓
数据合并 → unified_data.csv (122k行)
    ↓
┌────┴────┐
│         │
↓         ↓
因素分析  趋势分析
│         │
└────┬────┘
     ↓
Cox+ML模型训练
     ↓
结果可视化 → cox_ml_unified_results.png
     ↓
模型保存 → models_cox_ml/
```

---

## 四、遇到的问题与解决方案

### 问题1：两个数据源如何整合

**问题描述**: 
- 数据2有76,035条体检记录，但没有听力阈值，无法计算噪声聋目标变量
- 数据3有46,267条听力数据，有双耳高频平均听阈
- 两个数据源字段不同，无法直接合并

**解决方案**: 
在 `data_clear.py` 中按列索引提取各字段，统一列名后合并，添加"数据来源"标识

**最终逻辑**: 
```python
# 数据3: 有听力 → 可计算目标变量 (听阈>25dB)
df3['target_noise'] = (df3['双耳高频平均听阈结果'] > 25).astype(int)

# 数据2: 有职业病风险标签
df2['职业病风险'] = disease.str.contains('疑似职业病|职业病确诊').astype(int)

# 合并时保留各自目标变量
df_merged = pd.concat([df3, df2], ignore_index=True)
```

---

### 问题2：scikit-survival安装失败

**问题描述**: 
Cox模型需要scikit-survival库，但在Windows上安装失败（ecos依赖编译问题）

**尝试的方案**:
```
pip install scikit-survival  ❌ 失败
conda install scikit-survival  ❌ 未成功
```

**解决方案**: 
改用 `lifelines` 库，功能相同且安装简单
```
pip install lifelines  ✅ 成功
```

**代码调整**:
```python
# 之前
from sksurv.linear_model import CoxPHSurvivalAnalysis

# 之后
from lifelines import CoxPHFitter
cph = CoxPHFitter()
cph.fit(data, duration_col='duration', event_col='event')
```

---

### 问题3：Cox模型与ML模型输出不同

**问题描述**: 
- Cox模型输出风险比HR（生存分析视角）
- ML模型输出患病概率（二分类视角）
- 两者无法直接对比

**解决方案**: 
1. 将Cox风险分数通过sigmoid归一化到[0,1]
2. 简单加权平均融合

```python
# Cox输出: 风险分数 → 归一化
cox_scores_norm = 1 / (1 + np.exp(-cox_scores))

# 融合: 各50%
combined_proba = 0.5 * ml_proba + 0.5 * (1 - cox_scores_norm)
```

---

### 问题4：Cox模型AUC只有0.44

**问题描述**: 
训练出的Cox模型AUC=0.4354，远低于ML模型的0.67

**分析原因**: 
- Cox模型是生存分析模型，侧重"发病时间"
- 我们用接害工龄作为duration，但数据是横断面的
- 二分类任务中Cox模型本身就不占优势

**结论**: 
这是模型特性差异，不是bug。Cox的价值在于提供HR风险比分析，而非预测精度

---

### 问题5：特征权重如何计算

**问题描述**: 
需要综合Cox的HR和ML的特征重要性，但两者量纲不同

**解决方案**: 
在 `risk_model.py` 中使用Random Forest的feature_importances_作为权重

```python
# Random Forest特征重要性
imp = rf_model.feature_importances_
weights = imp / imp.sum()  # 归一化
```

**权重排序**:
| 特征 | 权重 |
|------|------|
| 接害工龄 | 0.358 |
| 性别 | 0.136 |
| BMI | 0.112 |
| 收缩压 | 0.099 |
| 体重 | 0.090 |
| 舒张压 | 0.090 |
| 身高 | 0.025 |

---

### 问题6：中文输出乱码

**问题描述**: 
控制台和图表中的中文显示为乱码（Windows环境）

**尝试的方案**:
```python
# 方案1: 设置字体
matplotlib.rcParams['font.sans-serif'] = ['SimHei']  ❌ 部分乱码

# 方案2: 使用其他编码
encoding='gbk'  ❌ 部分字符无法解码
```

**最终方案**: 
改用英文标签，彻底避免乱码

```python
feature_map = {
    '性别': 'Gender',
    '年龄': 'Age',
    '身高_cm': 'Height',
    '体重_kg': 'Weight',
    'BMI': 'BMI',
    '收缩压_mmHg': 'SBP',
    '舒张压_mmHg': 'DBP'
}
```

---

### 问题7：文件命名混乱

**问题描述**: 
开发过程中产生多个重复/废弃的脚本和文件

**清理前**:
```
├── survival_analysis.py      (废弃)
├── comprehensive_analysis.py  (废弃)
├── occupational_disease_model.py  (废弃)
├── risk_model.py             (重复)
├── data_clear.py
├── factor_analysis.py
├── cox_ml_unified.py
└── ... 多个旧的PNG和模型文件夹
```

**清理后**:
```
├── data_clear.py        # 数据处理
├── factor_analysis.py   # 因素分析
├── trend_analysis.py    # 趋势分析
├── cox_ml_unified.py    # Cox+ML模型
├── data/
├── models_cox_ml/
└── result/
```

---

### 问题8：趋势数据缺失

**问题描述**: 
`2020-2024年职业病新发报告时间.xlsx` 文件名暗示有5年数据，但实际只有2024年

**验证方法**:
```python
df = pd.read_excel('data/2020-2024年职业病新发报告时间.xlsx')
noise_df = df[df.iloc[:, 1] == df.iloc[:, 1].unique()[1]]
noise_df['年份'] = pd.to_datetime(noise_df.iloc[:, 3]).dt.year
print(noise_df['年份'].value_counts())
# 输出: 2024: 239  (只有2024年)
```

**结论**: 
无法做趋势分析，仅用于展示2024年职业病分布

---

## 五、关键决策记录

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 生存分析库 | lifelines | scikit-survival安装失败 |
| 特征数量 | 8个 | 基础体检指标，数据完整性高 |
| 目标变量定义 | 听阈>25dB | 职业性噪声聋诊断标准 |
| 模型融合方式 | 简单平均50%:50% | 尝试探索，效果待改进 |
| 输出语言 | 英文 | 避免中文乱码 |

---

## 六、代码执行流程

```bash
# 步骤1: 数据加载与合并
python data_clear.py
# 输入: data2.xlsx + 数据3.xlsx
# 输出: result/unified_data.csv

# 步骤2: 因素分析
python factor_analysis.py
# 输入: unified_data.csv
# 输出: unified_factor_analysis.png

# 步骤3: 趋势分析
python trend_analysis.py
# 输入: 2020-2024年职业病新发报告时间.xlsx
# 输出: occupation_disease_trend.png

# 步骤4: Cox+ML模型
python cox_ml_unified.py
# 输入: unified_data.csv
# 输出: cox_ml_unified_results.png + models_cox_ml/
```

---

## 七、模型性能汇总

| 模型 | AUC | 说明 |
|------|-----|------|
| Logistic Regression | 0.6676 | 基准模型 |
| Random Forest | 0.6691 | 可输出特征重要性 |
| **Gradient Boosting** | **0.6693** | 最佳模型 |
| Cox PH | 0.4354 | 生存分析视角 |
| Combined | 0.5729 | 融合效果待改进 |

---

## 八、核心发现

1. **接害工龄是最重要的风险因素**（权重36%）
2. **性别差异显著**：男性风险是女性的1.86倍（HR=1.857）
3. **BMI与疾病正相关**：每增加1单位，风险增加1.4%
4. **ML模型预测能力优于Cox模型**：AUC 0.67 vs 0.44
5. **融合策略需要优化**：简单平均效果一般

---

## 九、局限性与改进方向

### 局限性
- Cox模型与ML模型的信息重叠，融合提升有限
- 缺少噪声暴露剂量、工种等关键特征
- 横断面数据无法真正做生存分析

### 改进方向
- 引入更多特征（噪声暴露水平、防护措施等）
- 尝试Stacking等更复杂的融合策略
- 收集纵向数据，实现真正的Cox生存分析

---

*对话记录整理于 2026-05-07*